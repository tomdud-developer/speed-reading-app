server:
  port: 8081

spring:
  cloud:
    vault:
      host: localhost
      port: 8200
      authentication: TOKEN
      token: ${VAULT_TOKEN}
      fail-fast: true
      scheme: http
      kv:
        enabled: true
        application-name: speedreadingapp
